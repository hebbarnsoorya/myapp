global:
  scrape_interval: 5s   # How often to scrape targets

scrape_configs:
  - job_name: 'ppl-management'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
        labels:
          application: 'ppl-management'
